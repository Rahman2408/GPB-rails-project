

<%= form_tag new_project_path(current_user.id), :method => "get" do%>
    <p> 
        <%= label_tag :search, "Search by Name or Email:"  %>
        <%= text_field_tag :query, params[:query]  %>
        <%= submit_tag("Search", :name => nil) %>
    </p><br><br>
    <div class="selection">
    <%= collection_check_boxes(:project, :partner_ids, User.search(params[:query]).where.not(id: current_user.id), :id, :name  ) %>
    </div>

<% end%>

    
<%#@partners.each do |p| %>
    <%# render partial: "groups/partners", locals: {partner: p } %>
    <%# end%>
 
