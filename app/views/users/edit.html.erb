<h1>Update Your Profle</h1>

<%= form_for @user, url: user_path(@user) , method: "patch"  do |f| %>
    <%= f.label :name, "Name:" %>
    <%= f.text_field :name %><br>
    <%= f.label :email, "Email:" %>
    <%= f.text_field :email %><br>
    <%= f.submit "Update Pofile"%>
<% end %>