<h1>Your Project Partners</h1>


<% if @common_partners.size > 0 && !@common_partners.nil? %>
    <ul>
    <% @common_partners.each do |p|%>
      <%= link_to p.name, user_path(p) %>
    <% end%>
    </ul>
  <% else%>
    <h3>GET SOME FRIENDS!!</h3>
<% end%>

